class WastePost {
  String imageURL;
  int quantity;
  double latitude;
  double longitude;
  DateTime date;

  WastePost(
      {this.imageURL, this.quantity, this.latitude, this.longitude, this.date});
}